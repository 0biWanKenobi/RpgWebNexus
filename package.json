{
  "name": "rpg_web_nexus",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:dev": "vite build --watch --mode=development",
    "cloudflare:publish": "node ./scripts/prepare-shared.mjs && npm ci --progress=false && npm run build",
    "preview": "vite preview",
    "check:app": "svelte-check --tsconfig ./tsconfig.app.json",
    "check:node": "tsc --noEmit -p tsconfig.node.json",
    "check:fn": "tsc --noEmit --incremental false --composite false -p tsconfig.functions.json",
    "check": "npm run check:app && npm run check:node && npm run check:fn",
    "test:api": "wrangler pages dev ./dist --ip 127.0.0.1 --port 5173 --inspector-port 9229",
    "build:cloudfn": "tsc -p tsconfig.backend.json",
    "yalc:add:shared": "run-os",
    "yalc:add:shared:win32": "yalc add rpg_shared",
    "yalc:add:shared:linux:darwin": "${YALC_BIN:-$HOME/.local/bin/yalc} add rpg_shared"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/google.accounts": "^0.0.18",
    "@types/node": "^24.12.0",
    "run-script-os": "^1.1.6",
    "svelte": "^5.55.4",
    "svelte-check": "^4.4.6",
    "typescript": "^6.0.3",
    "vite": "^8.0.1",
    "wrangler": "^4.107.0"
  },
  "dependencies": {
    "rpg_shared": "file:.yalc/rpg_shared"
  }
}
